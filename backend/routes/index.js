const service = require('./service');
const product = require('./product');
const combo = require('./combo');
const servicesEmployees = require('./services_employees');

module.exports = {
  service,
  product,
  combo,
  servicesEmployees,
};
